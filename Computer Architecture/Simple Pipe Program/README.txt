This program spawns a child process, which reads and prints to terminal what was written by the parent process with the use of a pipe. 

Error: There is a redunacy in where the parent steps 1 - 3 are printed out twice.
